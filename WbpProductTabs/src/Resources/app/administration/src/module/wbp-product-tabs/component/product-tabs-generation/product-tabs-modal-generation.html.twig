{% block sw_product_tabs_modal_generation_modal %}
    <sw-modal class="wbp-product-tabs-modal-generation-modal-content"
              @modal-close="$emit('close-modal')">

        {% block wbp_product_tabs_modal_generation_sidebar %}

            <div>
                <sw-text-field
                        required
                        class="wbp-product-tabs-tabsName"
                        v-model="tabs.tabsName"
                        :label="$tc('wbp-product-tabs.general.tabsName')"
                >
                </sw-text-field>

            </div>

            <div>
                <sw-text-editor
                        required
                        class="wbp-product-tabs-data"
                        type="textarea"
                        :label="$tc('wbp-product-tabs.general.data')"
                        v-model="tabs.data">
                </sw-text-editor>

            </div>
                <div v-if="tabs.id == null">
                    <sw-button @click="saveTabs()">{{ $tc('wbp-product-tabs.general.save') }}</sw-button>
                </div>
                <div v-else>
                    <sw-button @click="editTab()">{{ $tc('wbp-product-tabs.general.edit') }}</sw-button>
                </div>


        {% endblock %}

    </sw-modal>

{% endblock %}